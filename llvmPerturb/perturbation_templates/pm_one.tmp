#include <stdlib.h>
#include <time.h>
#include <stdio.h>

int pone(){{
  int prob = {probability};
  srandom(time(0));
  if (random()% 100 < prob){{
    return {minus}1;
  }}else{{
    return 0;
  }}
}}
