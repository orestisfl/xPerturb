#include <stdlib.h>
#include <time.h>
//#include <sys/time.h>

#include <stdio.h>
#include "pone.h"

void init_pone(){{
  srandom(time(0));
}}

int pone(){{ // Perturbe namechange
  int prob = {probability};
  int r = random()% 100;
  if (r < prob){{
    return {minus}1;
  }}else{{
    return 0;
  }}
}}
